// Legal page constants — the one place the owner edits before production.

// TODO: set before production — the real inbox for privacy/legal requests
// (e.g. privacy@<your-domain>). Rendered on /privacy and /terms.
export const LEGAL_CONTACT_EMAIL = "MAIL_TBD";

/** Shown at the top of both legal pages. Update when the text materially changes. */
export const LEGAL_EFFECTIVE_DATE = "September 3, 2026";
