/* ── GOOGLE roofSegmentStats — ЧТЕНИЕ (roofcore) ──────────────────────────────
 *
 * От арбитра состава (блок 2 старой линии) осталось только чтение: движок
 * сегментации удалён вместе с построением, судить больше нечего. Сегменты
 * Google читаются целиком и кладутся в провенанс замера как данные —
 * азимут, уклон, площадь ската, центр в кадре.
 */

export interface ArbiterSegment {
  /** Компас-азимут ската Google, градусы. */
  azDeg: number;
  /** Уклон, градусы. */
  pitchDeg: number;
  /** Площадь СКАТА (наклонная), sq ft. */
  areaSf: number;
  /** Центр сегмента в кадре, ft (проекция от origin вызывающим). */
  xFt: number;
  yFt: number;
}
