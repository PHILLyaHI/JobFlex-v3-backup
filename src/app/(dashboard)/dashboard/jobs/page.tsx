import { ComingSoon } from "@/components/ui/ComingSoon";

export default function JobsPage() {
  return (
    <ComingSoon
      eyebrow="Delivery"
      title="Jobs"
      description="Every accepted proposal becomes a job — track status, installers, before/after photos, and expenses."
    />
  );
}
